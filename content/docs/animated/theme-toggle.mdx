---
title: Theme Toggle
description: Switch themes with rectangle, circle, or blurred-circle view transitions.
---

<ComponentPreview name="animated-theme-toggle-demo" />

## Overview

Theme Toggle uses the View Transition API when available and falls back to an immediate `next-themes` update. The icon swaps in place while the new theme reveals from a chosen origin.

## Installation

<CliBlock commands={["theme-toggle"]} />

## Usage

```tsx
import { ThemeToggle } from "@/components/animated/theme-toggle"

<ThemeToggle variant="circle-blur" start="center" />
```

Wrap your app with `ThemeProvider` from `next-themes` before using the component.

## Accessibility

The control updates its accessible name to describe the next theme. Reduced motion and unsupported browsers switch themes without the page reveal.

## API Reference

<ApiTable>
  <ApiRow name="variant" type='"rectangle" | "circle" | "circle-blur"' default='"rectangle"'>Page reveal shape.</ApiRow>
  <ApiRow name="start" type='"top-left" | "top-right" | "bottom-left" | "bottom-right" | "center" | "bottom-up"' default='"bottom-up"'>Reveal origin.</ApiRow>
  <ApiRow name="iconClassName" type="string">Class shared by the sun, moon, and hydration placeholder.</ApiRow>
</ApiTable>
