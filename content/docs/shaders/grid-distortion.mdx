---
title: Grid Distortion
description: Distort an image through a spring-like pointer-responsive displacement grid.
---

<ComponentPreview name="grid-distortion-demo" playground />

## Overview

Grid Distortion turns pointer movement into a damped displacement texture over an image. Use it for editorial covers, product artwork, or a single interactive image that rewards exploration.

## Installation

<CommandBlock commands={["honestui"]} />

## Usage

```tsx
import { GridDistortion } from "honestui/shaders"

<div className="h-80 overflow-hidden rounded-xl">
  <GridDistortion
    imageSrc="/images/cover.jpg"
    grid={18}
    strength={0.22}
    relaxation={0.92}
  />
</div>
```

## Accessibility

The canvas does not expose the source image to assistive technology. If the image is meaningful, render an equivalent semantic image or description nearby. Do not make pointer distortion the only way to discover information.

## API Reference

<ApiTable>
  <ApiRow name="imageSrc" type="string" required>URL of the image used as the texture.</ApiRow>
  <ApiRow name="grid" type="number" default="15">Resolution of the displacement grid.</ApiRow>
  <ApiRow name="mouse" type="number" default="0.1">Radius of pointer influence.</ApiRow>
  <ApiRow name="strength" type="number" default="0.15">Strength of the pointer displacement.</ApiRow>
  <ApiRow name="relaxation" type="number" default="0.9">Rate at which the image settles.</ApiRow>
  <ApiRow name="className" type="string">Class for sizing and positioning the container.</ApiRow>
</ApiTable>
