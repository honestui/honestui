---
title: Button
description: Trigger an action, submit a form, or open another control.
---

<ComponentPreview name="button-demo" />

## Overview

Use a button for actions. A button should do something on the current page, submit a form, open a menu, start a task, or confirm a choice.

Use a link when the user is navigating to another page. If a link must look like a button, render the link through the button component so the visual style is shared while the semantics stay correct.

## Anatomy

A button has a visible label, optional icon, variant, size, and state. The label should describe the action directly. Prefer `Save changes`, `Invite member`, or `Delete project` over vague labels like `Submit` or `Continue` when the result is specific.

## Variants

Use the default variant for primary actions, secondary for supporting actions, outline for medium emphasis, ghost for low emphasis, link for inline actions, and destructive for dangerous actions.

## Appearances

Use the `appearance` prop to change a button's surface treatment without changing its semantic variant or color. Choose `flat` to keep the original treatment, `glossy` for a polished sheen, `glow` for an internally lit treatment, or `bevel` for an extruded key-like treatment.

Appearances compose with every variant. For example, `variant="destructive" appearance="glossy"` keeps the destructive color and behavior while adding the glossy depth treatment.

## Accessibility

Buttons must have accessible names. Icon-only buttons need a label through text, `aria-label`, or an equivalent pattern. Keep disabled and loading states clear so users understand why the action is unavailable.

## Installation

<CodeTabs>

<TabsList>
  <TabsTab value="cli">CLI</TabsTab>
  <TabsTab value="manual">Manual</TabsTab>
</TabsList>
<TabsPanel value="cli">

```bash
npx honestui@latest add button
```

</TabsPanel>

<TabsPanel value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @base-ui-components/react
```

<Step>Import the following variables into your CSS file</Step>

```css
@theme inline {
  --color-destructive-foreground: var(--destructive-foreground);
}

:root {
  --destructive-foreground: oklch(0.505 0.213 27.518);
}

.dark {
  --destructive-foreground: oklch(0.704 0.191 22.216);
}
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="button" title="components/ui/button.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsPanel>

</CodeTabs>

## Usage

```tsx
import { Button } from "@/components/ui/button";
```

```tsx
<Button>Button</Button>
```

## Link

You can use the [`render`](https://base-ui.com/react/utils/use-render#migrating-from-radix-ui) prop to make another component look like a button. Here's an example of a link that looks like a button.

```tsx
import Link from "next/link";

import { Button } from "@/components/ui/button";

export function LinkAsButton() {
  return <Button render={<Link href="/login" />}>Login</Button>;
}
```

## Examples

The main example compares semantic variants, sizes, an icon-only action, disabled state, and link composition in one place. The examples below focus on behavior that needs its own code path.

### Surface appearances

Use appearance sparingly. It changes visual depth, not the action's meaning or priority.

<ComponentPreview name="button-appearances" />

### Loading state

Keep the button label stable enough that people know which action is pending. Disable duplicate submission while work is in progress, and expose the same state with `aria-disabled` or `disabled` as appropriate.

<ComponentPreview name="button-loading" />

### Icon placement

<ComponentPreview name="button-with-icon" />

## API reference

`Button` accepts native button props and Base UI's `render` composition prop. Honest UI adds `variant` (`default`, `outline`, `secondary`, `destructive`, `destructive-outline`, `ghost`, or `link`), `size` (`xs`, `sm`, `default`, `lg`, `xl`, `icon`, `icon-sm`, or `icon-lg`), and `appearance` (`flat`, `glossy`, `glow`, or `bevel`).

The native default is `type="button"`. Set `type="submit"` explicitly inside a form. Use `render` or `asChild` to preserve link semantics when a navigation action needs button styling.
