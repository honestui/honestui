// The SVG registry is compiled into React components by love-ui. Re-exporting
// that catalog keeps Honest UI's icon names and metadata in sync without
// shipping thousands of raw SVG files to consumers.
export * from "love-ui/icons"
