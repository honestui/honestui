// The SVG registry is compiled into React components by love-ui. Re-exporting
// that catalog keeps Honest UI's vector names and metadata in sync without
// shipping the raw SVG files to consumers.
export * from "love-ui/vectors"
