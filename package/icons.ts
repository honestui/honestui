// The SVG registry is compiled into React components by love-ui. Re-exporting
// that catalog keeps this package's icon names and metadata in sync with the
// source registry without shipping thousands of raw SVG files to consumers.
export * from "love-ui/icons";
